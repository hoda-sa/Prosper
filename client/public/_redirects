# Handle React Router client-side routing
# This redirects all requests to index.html except for files that actually exist

/*    /index.html   200

# Alternative syntax that's more explicit:
# /* /index.html 200

# If you want to be more specific, you can list each route:
# /transactions /index.html 200
# /budgets /index.html 200
# /accounts /index.html 200
# /profile /index.html 200
# /test-connection /index.html 200